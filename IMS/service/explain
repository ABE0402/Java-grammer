이 Service 부분은 입력 받은 데이터를 처리하는 부분이다.
IdolGroupListService 클래스는 IdolGroupListAction과 연결되어 아이돌 그룹 배열 데이터를 반환하는 역할을 합니다

IdolGroupRegistService 클래스는 새로운 아이돌 그룹을 등록할 때 중복을 체크하고, 
배열을 동적으로 확장하는 역할을 수행합니다. 
1. 중복 체크
   *이미 존재하는 아이돌 그룹 이름이 있는지 확인하여 중복이면 false 반환.

2. 배열 확장 및 복사
   *기존 배열을 tempArray에 저장 
   *새 배열을 생성해 기존 배열보다 크기를 1 늘림
   *기존 값을 새 배열로 복사
   *마지막 인덱스에 새로 등록할 아이돌 그룹 저장

3. 결과 반환
   *등록에 성공했으면 true, 아니면 false
